// Deprecated compatibility surface.
// Runtime mock rows were retired in W3 Task 3.1.
// Keep this one re-export temporarily so the existing feedback settings route
// can consume product configuration without carrying any mock/demo records.
export * from "@/config/aiChatbotProductConfig";
