name: Director Return-to-AI Resume Final
# Workflow 3 authorized targeted production smoke trigger — 2026-09-06

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/director-return-to-ai-resume-final.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  return-to-ai-resume-final:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PROD_ORIGIN: https://console-chat-hub.lovable.app
      CHANNEL_ID: b0000000-0000-0000-0000-000000000001
      PROJECT_REF: nrfxhqabwblzxoushgnm
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Run Return-to-AI real reply resume gate
        shell: bash
        run: bash scripts/director-return-to-ai-resume-final.sh
